#include "split.h"

std::vector<std::string> split(std::string str, std::string splitToken){
  std::vector<std::string> splitVect;
  int tempPos;
  while(!(str.find(splitToken) = std::string::npos)){
    tempPos = str.find(splitToken);
  }
  
  
}
